[RUNNING] 15. Sikeres só vásárlás teszt
>>>[Test15].run(scanner)
  >>>[CleanerRole].CleanerRole()
    >>>[Snowplow].Snowplow()
      >>>[ThrowerHead].ThrowerHead()
      <<<return
    <<<return
  <<<return
  >>>[Store].Store(inventory)
  <<<return
  >>>[CleanerRole].buy(item)
    >>>[Store].buy(cleanerRole, item)
      >>>[Buyable].getPrice()
      <<<return
      >>>[CleanerRole].getMoney()
      <<<return
      Decision: Elegendő a pénz a vásárláshoz? (1: Igen, 2: Nem)
      >> 1
      >>>[CleanerRole].decreaseMoney(price)
      <<<return
      >>>[CleanerRole].addSalt(amount)
        >>>[Snowplow].addSalt(amount)
          [STATE] saltStock értéke megnövelve.
        <<<return
      <<<return
    <<<return true
  <<<return
<<<return
[RESULT] The test completed successfully.
