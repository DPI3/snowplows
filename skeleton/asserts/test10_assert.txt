[RUNNING] 10. Takarítás sószóró fejjel teszt
>>>[Test10].run(scanner)
    >>>[CleanerRole].CleanerRole()
        >>>[Snowplow].Snowplow()
            >>>[ThrowerHead].ThrowerHead()
            <<<return
        <<<return
    <<<return
    >>>[Lane].Lane()
    <<<return
    >>>[SaltSpreaderHead].SaltSpreaderHead()
    <<<return
    >>>[Snowplow].Snowplow()
        >>>[ThrowerHead].ThrowerHead()
        <<<return
    <<<return
    >>>[Snowplow].changeHead(newHead)
    <<<return
    >>>[CleanerRole].controlSnowplow([Snowplow])
    <<<return
    >>>[Snowplow].clean(lane)
        >>>[SaltSpreaderHead].clean(lane, snowplow)
        <<<return
    <<<return
    >>>[Game].tick()
    <<<return
<<<return
[RESULT] The test completed successfully.