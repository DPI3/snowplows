[RUNNING] 23. Autó haladása járható sávban teszt
>>>[Game].tick()
  >>>[Car].tick()
    >>>[Car].move()
      >>>[Route].getNextLane(cl)
      <<<return nl
      Decision: A sáv járható? (1: Igen, 2: Nem)
      >> 1
      >>>[Lane].isPassable()
      <<<return true
      >>>[Car].setCurrentLane(nl)
      <<<return
      >>>[Car].setPositionOnLane(newPosition)
      <<<return
    <<<return
  <<<return
<<<return
[RESULT] The test completed successfully.
