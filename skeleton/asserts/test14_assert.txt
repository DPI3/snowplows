[RUNNING] 14. Sikeres biokerozin vásárlás teszt
>>>[Test14].run(scanner)
  >>>[CleanerRole].CleanerRole()
    >>>[Snowplow].Snowplow()
      >>>[ThrowerHead].ThrowerHead()
      <<<return
    <<<return
  <<<return
  >>>[Store].Store(inventory)
  <<<return
  >>>[CleanerRole].getSnowplow()
  <<<return
  >>>[Snowplow].getBiokeroseneStock()
  <<<return 0
  >>>[CleanerRole].buy(item)
    >>>[Store].buy(cleanerRole, item)
      >>>[Buyable].getPrice()
      <<<return
      >>>[CleanerRole].getMoney()
      <<<return
      Decision: Elegendő a pénz a vásárláshoz? (1: Igen, 2: Nem)
      >> 1
      >>>[CleanerRole].decreaseMoney(price)
      <<<return
      >>>[CleanerRole].addBiokerosene(amount)
        >>>[Snowplow].addBiokerosene(amount)
          [STATE] biokeroseneStock értéke megnövelve.
        <<<return
      <<<return
      [STATE] Sikeres vásárlás.
    <<<return true
  <<<return true
  >>>[Snowplow].getBiokeroseneStock()
  <<<return 10
  [STATE] A hókotró biokerozin készlete nőtt.
<<<return
[RESULT] The test completed successfully.