[RUNNING] 24. Autó elakadása járhatatlan úton teszt
>>>[Car].Car()
<<<return
>>>[Vehicle].setCurrentLane(lane)
<<<return
>>>[Game].tick()
  [STATE] currentRound növelve: 1
  >>>[Car].tick()
    >>>[Car].move()
      >>>[Route].getNextLane(cl)
      <<<return nl
      Decision: A sáv járható? (1: Igen, 2: Nem)
      >> 2
      >>>[Lane].isPassable()
      <<<return false
      >>>[Car].stopAndWait()
        [STATE] Speed set to 0, position remains
      <<<return
    <<<return
  <<<return
<<<return
[RESULT] The test completed successfully.
