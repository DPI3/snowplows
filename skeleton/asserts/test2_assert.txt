[RUNNING] 2. Havazás hatása az útállapotra teszt
Decision: Mennyi hó esett?
1: Kevés
2: Sok
>> 1
>>>[Weather].snowfallTick(roadNetwork)
    >>>[RoadNetwork].weatherEffects()
        >>>[Road].applyWeatherEffects()
            >>>[Lane].applyWeather(snowAmount)
                >>>[LaneState : currentState].handleWeatherChange(snowAmount)
                <<<return ThinSnow
                >>>[Lane].changeState(ThinSnow)
                    [STATE] Lane.state = ThinSnow
                <<<return
            <<<return
        <<<return
    <<<return
<<<return
[RESULT] The test completed successfully.